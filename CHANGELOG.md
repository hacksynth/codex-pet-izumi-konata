# Changelog / 更新日志

All notable changes are documented here. Pet versions are defined by Git tags and GitHub
Releases; `pet.json` intentionally contains no separate version field.

所有重要变更记录于此。宠物版本由 Git 标签和 GitHub Release 定义；`pet.json` 不添加独立版本字段。

## Unreleased / 未发布

- Add open-source project governance, validation tooling, CI, security automation, and
  tagged release packaging.

## Initial release / 初始版本

- Add the installable Izumi Konata Codex v2 pet and QA artifacts.
